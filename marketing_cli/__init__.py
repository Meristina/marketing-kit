"""marketing_cli — the Marketing-Kit CLI: scaffold the toolkit into a project
(`marketing init`) and run a headless mission (`marketing run`) over the unchanged
`marketing_kit` engine."""

__version__ = "0.1.0"
