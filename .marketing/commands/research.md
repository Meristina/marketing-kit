---
description: Phase 1 — insight & research (Officer 1)
argument-hint: "<mission dir, e.g. missions/001-...>"
---

# /marketing.research — Phase 1 (Officer 1 · Insight & Research)

**Constitution:** re-read `.marketing/memory/constitution.md` (esp. Art. I sourcing — never
fabricate a metric/market size, IV shared arsenal).

`$MISSION` = `$ARGUMENTS`.

## Do
1. **Read in** `$MISSION/dossier.md` (goal, context) — don't re-ask.
2. **Delegate to the `officer-1-research` subagent** (Agent tool — runs on its grade model; else
   adopt its role inline + load the relevant skills). Pick only the soldiers this brief needs:
   **jtbd** (the customer's job, usually first) · **category-entry-points** (CEPs / mental
   availability) · **persona-segmentation** (segments + personas) · **market-sizing** (TAM/SAM/SOM)
   · **ai-synthetic-audience** (exploratory hypotheses only — never evidence).
3. Produce a **sourced insight base**: the job(s), the CEPs, segment map, market size, and a
   **baseline** of current marketing metrics where they exist. Every figure cites a real source;
   never fabricate.
4. **Write out** `$MISSION/research.md`; update the Dossier → set `baseline`, add the decision,
   sources, assumptions (tagged), and open-to-verify items.

## Done when
`$MISSION/research.md` exists with a sourced job/CEPs/segments/sizing + baseline, and the Dossier
is updated. Mirror the user's language.
