---
description: Phase 4 — demand & activation, short-term (Officer 4)
argument-hint: "<mission dir, e.g. missions/001-...>"
---

# /marketing.demand — Phase 4 (Officer 4 · Demand & Activation)

**Constitution:** re-read `.marketing/memory/constitution.md` (esp. Art. XI — run with brand for
the 60:40 balance; incrementality-first, never last-click alone).

`$MISSION` = `$ARGUMENTS`. Runs the **short-term** half of the 60:40 balance — alongside
`/marketing.brand`.

## Do
1. **Read in** the Dossier + `strategy.md` (target, positioning, value prop) + brand assets/CEPs.
2. **Delegate to the `officer-4-demand` subagent**. Pick what the brief needs: **value-prop-design**
   (the promise; shared with Officer 2) · **full-funnel-paid-media** (channel mix + flighting +
   incrementality-first measurement) · **conversion-cro** · **plg** (product-led growth) ·
   **content-seo** · **lead-scoring-lifecycle** (handoff to Officer 5).
3. Produce the activation plan: the promise, channel/funnel plan, conversion plan, growth loops,
   lead handoff — with an **incrementality-first measurement plan** (never last-click alone).
4. **Write out** `$MISSION/demand.md`; update the Dossier.
5. **Gate**: run `/marketing.gate demand`. On `GATE: FAIL`, fix and re-gate.

## Done when
`$MISSION/demand.md` holds the channel + conversion + handoff plan, the Dossier is updated, and
`/marketing.gate demand` returned `GATE: PASS`. The 60:40 split itself is Officer 6's. Mirror the
user's language.
